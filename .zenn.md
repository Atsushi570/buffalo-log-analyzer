# Zenn記事の管理

`articles/` と `images/` は [Zenn CLI](https://zenn.dev/zenn/articles/zenn-cli-guide) の規約に従っています。

## 公開手順

1. Zennのダッシュボードで、このGitHubリポジトリを連携する
2. 記事の frontmatter を `published: true` に変更して push すると公開される

## ローカルでプレビュー

初回のみ依存をインストールします。

```bash
npm install
```

以降はこれでプレビューできます。

```bash
npx zenn preview      # http://localhost:8000
npm run preview       # 同じ（package.json の scripts 経由）
```

新しい記事を作るとき:

```bash
npx zenn new:article
```

> `zenn-cli` は devDependencies なので、GitHub Pages で配信される
> サイト本体（`index.html` / `src/`）には影響しません。ビルドも不要のままです。
